<UserControl x:Class="TrailMeisterViewer.Windows.MainWindow.PeopleTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TrailMeisterViewer.Windows.MainWindow"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DataGrid x:Name="gridPeople"
                  Grid.Row="0"
                  ItemsSource="{Binding DataContext.AllPeople, ElementName=gridPeople}"
                  IsSynchronizedWithCurrentItem="False"
                  FontSize="24" AutoGenerateColumns="False" GridLinesVisibility="All" HorizontalAlignment="Stretch"
                  CanUserSortColumns="True"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn x:Name="colPeopleID" CanUserResize="False" Width="1*" Header="ID" Binding="{Binding PersonId}" IsReadOnly="true" MinWidth="50"
                                    HeaderStyle="{StaticResource DataGridHeaderRight}"
                                    ElementStyle="{StaticResource DataGridCellRight}"/>

                <DataGridTemplateColumn x:Name="colPeopleFirstName" Width="4*" Header="First Name" IsReadOnly="false" MinWidth="300"
                                        HeaderStyle="{StaticResource DataGridHeaderLeft}"
                                        SortMemberPath="FirstName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox x:Name="tbFirstNameEdit" FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}"
                                     Text="{Binding FirstName, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                     BorderThickness="0" Margin="8,0,8,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn x:Name="colPeopleLastName" Width="4*" Header="Last Name" IsReadOnly="false" MinWidth="300"
                                        HeaderStyle="{StaticResource DataGridHeaderLeft}"
                                        SortMemberPath="LastName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox x:Name="tbLastNameEdit" FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}"
                                     Text="{Binding LastName, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                     BorderThickness="0" Margin="8,0,8,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn x:Name="colPeopleNickName" Width="4*" Header="Nickname" IsReadOnly="false" MinWidth="300"
                                        HeaderStyle="{StaticResource DataGridHeaderLeft}"
                                        SortMemberPath="NickName">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox x:Name="tbNickNameEdit" FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}"
                                     Text="{Binding NickName, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                     BorderThickness="0" Margin="8,0,8,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>

                <DataGridTemplateColumn x:Name="colPeopleAssociation" Width="4*" Header="Association" IsReadOnly="false" MinWidth="300"
                                        HeaderStyle="{StaticResource DataGridHeaderLeft}"
                                        SortMemberPath="Association">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox x:Name="tbAssociation" FocusManager.FocusedElement="{Binding RelativeSource={RelativeSource Self}}"
                                     Text="{Binding Association, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                     BorderThickness="0" Margin="8,0,8,0"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Button x:Name="btnAddPerson"
                Content="Add Person"
                Grid.Row="1"
                HorizontalAlignment="Right" VerticalAlignment="Bottom" Height="48" Width="120"
                Command="{Binding Path=ButtonCommand_AddPerson}" Margin="0,8,8,8"
                Style="{StaticResource GlossyBeveledButton}"/>
    </Grid>
</UserControl>
