<UserControl x:Class="TrailMeisterViewer.Windows.PersonalLog.SummaryGrid"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrailMeisterViewer.Windows.PersonalLog"
             xmlns:converters="clr-namespace:TrailMeisterUtilities.Converters;assembly=TrailMeisterUtilities"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="parent">
    <UserControl.Resources>
        <converters:Person2NameConverter x:Key="Person2NameConverter"/>
        <converters:Laps2TimeConverter x:Key="Laps2TimeConverter"/>
        <converters:Ms2TimeConverter x:Key="Ms2TimeConverter"/>
        <converters:Laps2DistanceConverter x:Key="Laps2DistanceConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Label x:Name="lblTotal" Grid.Row="0" Content="{Binding Label, ElementName=parent}" Padding="2,2,2,2" />

        <local:SummaryItem Grid.Row="1" Label="Laps:" Data="{Binding LapCount}" />
        
        <local:SummaryItem Grid.Row="2" Label="Distance:">
            <local:SummaryItem.Data>
                <MultiBinding Converter="{StaticResource Laps2DistanceConverter}">
                    <Binding Path="Laps" />
                </MultiBinding>
            </local:SummaryItem.Data>
        </local:SummaryItem>

        <local:SummaryItem Grid.Row="3" Label="Time:">
            <local:SummaryItem.Data>
                <MultiBinding Converter="{StaticResource Laps2TimeConverter}">
                    <Binding Source="{x:Static converters:TimeConversionType.TotalTime}" />
                    <Binding Path="Laps"/>
                </MultiBinding>
            </local:SummaryItem.Data>
        </local:SummaryItem>

        <local:SummaryItem Grid.Row="4" Label="Best:">
            <local:SummaryItem.Data>
                <MultiBinding Converter="{StaticResource Laps2TimeConverter}">
                    <Binding Source="{x:Static converters:TimeConversionType.BestLap}" />
                    <Binding Path="Laps"/>
                </MultiBinding>
            </local:SummaryItem.Data>
        </local:SummaryItem>

        <local:SummaryItem Grid.Row="5" Label="Laps:">
            <local:SummaryItem.Data>
                <MultiBinding Converter="{StaticResource Laps2TimeConverter}">
                    <Binding Source="{x:Static converters:TimeConversionType.AverageLap}" />
                    <Binding Path="Laps"/>
                </MultiBinding>
            </local:SummaryItem.Data>
        </local:SummaryItem>
     
    </Grid>
</UserControl>
