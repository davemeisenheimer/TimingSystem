<UserControl x:Class="TrailMeisterViewer.Windows.PersonalLog.SummaryItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TrailMeisterViewer.Windows.PersonalLog"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             x:Name="parent">
    <UserControl.Resources>
        <Style TargetType="Label">
            <Setter Property="Padding" Value="2" />
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="60"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Content="{Binding Label, ElementName=parent}" />
        <Label Content="{Binding Data, ElementName=parent}" Grid.Column="1" />
    </Grid>
</UserControl>
