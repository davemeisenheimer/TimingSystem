<UserControl x:Class="TrailMeisterViewer.Windows.PersonalLog.SummaryItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:TrailMeisterViewer.Windows.PersonalLog"
             mc:Ignorable="d"
             x:Name="parent">

    <Grid Margin="0,2,0,2">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0"
                   Text="{Binding Label, ElementName=parent}"
                   Foreground="{StaticResource MutedBlueBrush}"
                   FontSize="13"
                   VerticalAlignment="Center"/>

        <TextBlock Grid.Column="1"
                   Text="{Binding Data, ElementName=parent}"
                   FontSize="14" FontWeight="SemiBold"
                   Foreground="{StaticResource DarkNavyBrush}"
                   VerticalAlignment="Center"/>
    </Grid>
</UserControl>
